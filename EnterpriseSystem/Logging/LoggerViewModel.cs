namespace EnterpriseSystem.Logging
{
    // contains the global and single logger object
    public static class LoggerViewModel
    {
        public static readonly Logger Logger = Logger.Instance;
    }
}
